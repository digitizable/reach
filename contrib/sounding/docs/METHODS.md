# Sounding methods

## Residual CONNECT

1. Prefer a known underlay when identity separation matters.
2. Require CONNECT 200 + exit geo + successful HTTPS for winners.
3. Expect high churn; re-harvest often.

## Invite handshake

Public TCP invite measures cooperative reverse interest. Classify self-tests (localhost, known egress, labeled tokens) separately from external YES.

## Mirage face probe

Naked control ports may speak the reverse grammar. Cover ports must not.
